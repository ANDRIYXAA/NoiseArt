// ============================================================================
// NoiseArt — LayersPanel (Панель шарів)
// ============================================================================
// Показує список шарів з можливістю:
// - Увімкнути/вимкнути шар (око)
// - Вибрати шар
// - Додати/видалити/дублювати шар
// - Змінити порядок (вверх/вниз)
// - Opacity та Blend Mode
// ============================================================================

#pragma once

#include <imgui.h>
#include "core/LayerStack.h"

namespace NoiseArt {

class LayersPanel {
public:
    /// Малює панель шарів
    /// @param stack Стек шарів для відображення та керування
    /// @return true, якщо щось змінилося (потрібно перемалювати)
    bool render(LayerStack& stack);

    bool isOpen = true;
};

} // namespace NoiseArt
